"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";

type Msg={id:number;authorName:string;authorRole:string;message:string;visibility:string;createdAt:string};
type Ticket={ticketCode:string;subject:string;category:string;description:string;priority:string;sensitivity:string;status:string;county:string;requesterName:string;requesterRole:string;assignedTeam:string;assignedTo:string;slaHours:number;dueAt:string;resolution:string;satisfaction:number;createdAt:string;messages:Msg[]};
type Article={articleCode:string;title:string;category:string;audience:string;summary:string;content:string};
const categories=["Account / access","Registration / data correction","Offline synchronization","GIS / mapping","Programme / voucher","Mobile money / payment","Privacy / consent","Grievance / safeguarding","Technical error","Training / how-to"];
const counties=["National","Bomi","Bong","Gbarpolu","Grand Bassa","Grand Cape Mount","Grand Gedeh","Grand Kru","Lofa","Margibi","Maryland","Montserrado","Nimba","River Cess","River Gee","Sinoe"];
const statuses=["Open","Acknowledged","In progress","Waiting for user","Resolved","Closed"];

export default function HelpDesk({role,notify}:{role:string;notify:(s:string)=>void}){const[data,setData]=useState<{tickets:Ticket[];articles:Article[];access:{canManage:boolean;role:string}}>({tickets:[],articles:[],access:{canManage:false,role}});const[tab,setTab]=useState("mine");const[modal,setModal]=useState(false);const[selected,setSelected]=useState<Ticket|null>(null);const[query,setQuery]=useState("");const[busy,setBusy]=useState(false);
 const load=async()=>{const r=await fetch("/api/help-desk");const j=await r.json();if(r.ok){setData(j);setSelected(s=>s?j.tickets.find((t:Ticket)=>t.ticketCode===s.ticketCode)||null:null)}};useEffect(()=>{load()},[]);
 const mine=data.tickets.filter(t=>!data.access.canManage||tab!=="mine"||t.requesterRole===role);const filtered=mine.filter(t=>(t.ticketCode+t.subject+t.category+t.requesterName).toLowerCase().includes(query.toLowerCase()));const open=data.tickets.filter(t=>!["Resolved","Closed"].includes(t.status)).length;const overdue=data.tickets.filter(t=>!["Resolved","Closed"].includes(t.status)&&new Date(t.dueAt)<new Date()).length;const resolved=data.tickets.filter(t=>t.status==="Resolved"||t.status==="Closed").length;
 async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/help-desk",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...body,requesterRole:role})});const j=await r.json();setBusy(false);if(r.ok){setModal(false);notify(`Support request ${j.ticketCode} submitted.`);await load()}else notify(j.error||"Request failed")}
 async function action(body:Record<string,unknown>,method="PATCH"){setBusy(true);const r=await fetch("/api/help-desk",{method,headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json();setBusy(false);if(!r.ok)notify(j.error||"Action failed");else{notify("Help Desk record updated.");await load()}}
 return <div className="hd-space"><section className="hd-hero panel"><div><span>National user support centre</span><h2>Help Desk & Service Assurance</h2><p>Submit, monitor and resolve operational support requests with accountable ownership, SLA controls and a complete conversation history.</p></div><button onClick={()=>setModal(true)}>＋ New support request</button></section>
 <div className="metric-grid"><article className="metric"><div><span>Active requests</span><strong>{open}</strong><small>Awaiting action or response</small></div><i>◌</i></article><article className="metric"><div><span>SLA overdue</span><strong>{overdue}</strong><small>Requires immediate escalation</small></div><i>!</i></article><article className="metric"><div><span>Resolved</span><strong>{resolved}</strong><small>Closed-loop support</small></div><i>✓</i></article><article className="metric"><div><span>Guidance articles</span><strong>{data.articles.length}</strong><small>Published self-service answers</small></div><i>?</i></article></div>
 <div className="hd-tabs"><button className={tab==="mine"?"active":""} onClick={()=>setTab("mine")}>My requests</button>{data.access.canManage&&<button className={tab==="queue"?"active":""} onClick={()=>setTab("queue")}>Support queue</button>}<button className={tab==="knowledge"?"active":""} onClick={()=>setTab("knowledge")}>Knowledge base</button><input placeholder="Search tickets or guidance…" value={query} onChange={e=>setQuery(e.target.value)}/></div>
 {tab==="knowledge"?<section className="hd-kb">{data.articles.filter(a=>(a.title+a.category+a.summary).toLowerCase().includes(query.toLowerCase())).map(a=><article className="panel" key={a.articleCode}><span>{a.category}</span><h3>{a.title}</h3><p>{a.summary}</p><details><summary>Read guidance</summary><p>{a.content}</p></details><small>{a.articleCode} · {a.audience}</small></article>)}</section>:<section className="panel registry"><div className="table-tools"><div><b>{tab==="queue"?"National support queue":"My support requests"}</b><span>{filtered.length} accountable service records</span></div><button onClick={()=>setModal(true)}>＋ Submit request</button></div><div className="table-wrap"><table><thead><tr><th>Ticket</th><th>Request</th><th>Priority / SLA</th><th>Ownership</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(t=><tr key={t.ticketCode}><td><code>{t.ticketCode}</code><small>{new Date(t.createdAt).toLocaleDateString()}</small></td><td><b>{t.subject}</b><small>{t.category} · {t.county}</small></td><td><span className={`hd-priority ${t.priority.toLowerCase()}`}>{t.priority}</span><small className={new Date(t.dueAt)<new Date()&&!['Resolved','Closed'].includes(t.status)?"late":""}>Due {new Date(t.dueAt).toLocaleString()}</small></td><td>{t.assignedTeam}<small>{t.assignedTo}</small></td><td><span className="status">{t.status}</span></td><td><button className="approve" onClick={()=>setSelected(t)}>Open</button></td></tr>)}</tbody></table>{!filtered.length&&<div className="hd-empty"><b>No support requests found</b><p>Submit a request and the Help Desk will issue a traceable ticket number immediately.</p><button onClick={()=>setModal(true)}>New support request</button></div>}</div></section>}
 {modal&&<div className="modal-back"><form className="register-modal hd-form" onSubmit={create}><header><div><span>Accountable user support</span><h2>New support request</h2></div><button type="button" onClick={()=>setModal(false)}>×</button></header><div className="form-grid"><label>Category*<select name="category" required>{categories.map(x=><option key={x}>{x}</option>)}</select></label><label>Priority*<select name="priority"><option>Normal</option><option>Low</option><option>High</option><option>Critical</option></select></label><label className="wide">Subject*<input name="subject" required placeholder="Briefly describe the issue"/></label><label>County<select name="county">{counties.map(x=><option key={x}>{x}</option>)}</select></label><label>Preferred contact channel<select name="channel"><option>In-platform</option><option>Phone callback</option><option>Email follow-up</option></select></label><label>Sensitivity<select name="sensitivity"><option>Internal</option><option>Restricted personal data</option><option>Highly restricted</option></select></label><label className="wide">Details*<textarea name="description" required placeholder="What happened, what were you trying to do, and what record or reference is affected? Never submit a password or PIN."/></label></div><footer><button type="button" onClick={()=>setModal(false)}>Cancel</button><button disabled={busy}>{busy?"Submitting…":"Submit support request"}</button></footer></form></div>}
 {selected&&<TicketDrawer ticket={selected} canManage={data.access.canManage} busy={busy} close={()=>setSelected(null)} action={action}/>}</div>}

function TicketDrawer({ticket:t,canManage,busy,close,action}:{ticket:Ticket;canManage:boolean;busy:boolean;close:()=>void;action:(b:Record<string,unknown>,m?:string)=>void}){const[msg,setMsg]=useState("");const[visibility,setVisibility]=useState("Requester-visible");const[state,setState]=useState({status:t.status,assignedTeam:t.assignedTeam,assignedTo:t.assignedTo,priority:t.priority,resolution:t.resolution});return <div className="drawer-wrap"><aside className="delivery-drawer hd-drawer"><header><div><span>{t.category}</span><h2>{t.subject}</h2><code>{t.ticketCode}</code></div><button onClick={close}>×</button></header><section className="hd-ticket-summary"><div><b>{t.status}</b><small>Status</small></div><div><b>{t.priority}</b><small>Priority</small></div><div><b>{t.assignedTeam}</b><small>Assigned team</small></div><div><b>{new Date(t.dueAt).toLocaleString()}</b><small>SLA due</small></div></section><section><h3>Request</h3><p>{t.description}</p><small>{t.requesterName} · {t.requesterRole} · {t.county}</small></section><section><h3>Conversation & evidence</h3><div className="hd-thread">{t.messages.map(m=><div key={m.id} className={m.visibility==="Internal"?"internal":""}><b>{m.authorName}</b><span>{m.authorRole} · {m.visibility} · {new Date(m.createdAt).toLocaleString()}</span><p>{m.message}</p></div>)}</div><textarea value={msg} onChange={e=>setMsg(e.target.value)} placeholder="Add a response or progress update…"/>{canManage&&<select value={visibility} onChange={e=>setVisibility(e.target.value)}><option>Requester-visible</option><option>Internal</option></select>}<button disabled={busy||!msg.trim()} onClick={async()=>{await action({action:"message",ticketCode:t.ticketCode,message:msg,visibility},"POST");setMsg("")}}>Add update</button></section>{canManage?<section><h3>Support workflow controls</h3><div className="drawer-controls"><label>Status<select value={state.status} onChange={e=>setState({...state,status:e.target.value})}>{statuses.map(x=><option key={x}>{x}</option>)}</select></label><label>Priority<select value={state.priority} onChange={e=>setState({...state,priority:e.target.value})}><option>Low</option><option>Normal</option><option>High</option><option>Critical</option></select></label><label>Assigned team<select value={state.assignedTeam} onChange={e=>setState({...state,assignedTeam:e.target.value})}><option>Help Desk</option><option>Registry & Data Quality</option><option>GIS & Mapping</option><option>Programmes & Payments</option><option>Infrastructure & Security</option><option>Institutional Governance</option></select></label><label>Assigned officer<input value={state.assignedTo} onChange={e=>setState({...state,assignedTo:e.target.value})}/></label><label className="wide">Resolution<textarea value={state.resolution} onChange={e=>setState({...state,resolution:e.target.value})} placeholder="Required before resolving"/></label></div><button disabled={busy} onClick={()=>action({ticketCode:t.ticketCode,...state})}>Save workflow decision</button></section>:<section><h3>After resolution</h3>{["Resolved","Closed"].includes(t.status)?<><button onClick={()=>action({ticketCode:t.ticketCode,action:"reopen"})}>Reopen request</button><p>Rate this resolution: {[1,2,3,4,5].map(n=><button className="hd-star" key={n} onClick={()=>action({ticketCode:t.ticketCode,action:"rate",satisfaction:n})}>{n<=t.satisfaction?"★":"☆"}</button>)}</p></>:<p>Your request is being tracked. Add a reply above whenever the support team asks for clarification.</p>}</section>}</aside></div>}
